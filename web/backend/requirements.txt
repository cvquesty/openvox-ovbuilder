# Web backend dependencies (install into the same venv as ovbuilder, or a
# dedicated one). Pinned to known-good versions; run `pip-audit` in CI.
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
pydantic-settings>=2.0
# PyJWT replaces python-jose, which has multiple unpatched CVEs
# (e.g. algorithm confusion). PyJWT is actively maintained.
PyJWT>=2.8
passlib[bcrypt]>=1.7
python-multipart>=0.0.9
ldap3>=2.9
celery[redis]>=5.3
redis>=5.0
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
alembic>=1.13
httpx>=0.27
