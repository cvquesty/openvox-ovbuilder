# Copy to variables.auto.pkrvars.hcl (gitignored) — NEVER commit the copy.
# See docs/SECRETS.md

vcenter_server   = "vcenter.example.com"
vcenter_username = "administrator@vsphere.local"
# Set in the gitignored copy only. Empty here so detectors see no secret.
vcenter_password = ""

datacenter = "Main DC"
cluster    = "Production Cluster"
datastore  = "vsanDatastore"
network    = "VM Network"
folder     = "Templates"

iso_datastore = "isos"
insecure_connection = true

# Golden guest login (same value you put in ~/.config/ovbuilder/secrets.env
# as OVBUILDER_GOLDEN_PASSWORD for clones).
# Set in the gitignored copy only. Empty here so detectors see no secret.
ssh_password = ""

# Ubuntu autoinstall identity requires a crypt hash of the same password:
#   openssl passwd -6   # run locally; paste the hash into the gitignored copy
ssh_password_crypted = ""

# Optional:
# ssh_public_key = "ssh-ed25519 AAAA... you@example.com"
