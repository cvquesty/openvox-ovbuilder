# Packer local vars (secrets) — never commit
packer/variables.auto.pkrvars.hcl
packer/**/packer_cache/
*.pkrvars.hcl
!*.pkrvars.hcl.example

# Operator secrets (XDG copies are outside the repo; ignore if placed here)
secrets.env
secrets.yaml
**/secrets.env
**/secrets.yaml
.env
.env.*

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
build/
dist/

# Terraform local
**/.terraform/
*.tfstate
*.tfstate.*
crash.log
override.tf
*_override.tf
